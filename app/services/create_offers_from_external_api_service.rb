# frozen_string_literal: true

require 'external_api'

class CreateOffersFromExternalApiService
  def call
    # Some logic to fetch and create offers using data
    # from https://bravado.co/api/api/opportunity/intros.json
    #
    # In case the test task we assume we are able to syncronize data
    # with external api by periodical background jobs
  end
end
